const categoriesState = {
  categories: [],
};
const dataState = {
  data: [],
};
const userState = {
  user: {},
};
const isLoadingState = {
  isLoading: false,
};

export { isLoadingState, userState, dataState, categoriesState };
